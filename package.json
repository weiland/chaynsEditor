{
  "name": "chaynsEditor",
  "version": "0.7.0",
  "description": "WYSIWYG-Editor for LangRes in Chayns V3",
  "scripts": {
    "build": "gulp build",
    "qa": "gulp build && gulp qa",
    "release": "gulp build && gulp release"
  },
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-minify-css": "^1.1.0",
    "gulp-rename": "^1.2.2",
    "gulp-uglify": "^1.2.0",
    "gulp-util": "^3.0.4"
  }
}
